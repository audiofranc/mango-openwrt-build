config_format=v11
enabled="1"
luci_stats="1"
raw_block_lists="hagezi:light hagezi:tif.mini"
whitelist_mode="0"
local_blocklist_path="/etc/adblock-lean/blocklist"
local_whitelist_path="/etc/adblock-lean/whitelist"
max_file_part_size_KB="4000"
# DNSMASQ_INDEXES / DNSMASQ_CONF_DIRS intentionally omitted —
# let `service adblock-lean setup` auto-detect these on first boot,
# since the conf-dir path differs on 25.12.5 vs older OpenWrt.
